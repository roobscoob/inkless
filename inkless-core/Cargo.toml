[package]
name = "inkless-core"
version = "0.1.0"
edition = "2024"

[dependencies]
unicode-segmentation = { version = "1.12.0", features = ["no_std"] }
unicode-width = { version = "0.2.2", features = ["no_std"] }

[features]
default = ["std"]
std = ["alloc"]
alloc = []
